{
  "name": "ethereum-indexer-monorepo",
  "private": true,
  "version": "0.0.1",
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "syncpack": "^9.8.4"
  },
  "volta": {
    "node": "18.7.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "sync-deps": "syncpack fix-mismatches"
  }
}
