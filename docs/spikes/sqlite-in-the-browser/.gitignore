node_modules/
.harness-build/
test-results/
playwright-report/

# Derived from the committed stream fixture by `npx tsx run/verify-port.ts`, in
# seconds. The 12 MB trace regenerates identically and is not worth carrying;
# the golden STATE is kept (it is the oracle, and a diff on it means the
# processor changed meaning).
fixtures/*.trace.json
