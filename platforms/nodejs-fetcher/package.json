{
	"name": "@etherfold/platform-nodejs-fetcher",
	"version": "0.1.0",
	"description": "Node host adapter for the etherfold log-fetcher: a long-running process driving fetch cycles on an interval, with backoff. Configuration and scheduling only.",
	"publishConfig": {
		"access": "public"
	},
	"type": "module",
	"module": "dist/index.js",
	"types": "dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		},
		"./package.json": "./package.json"
	},
	"bin": {
		"etherfold-fetch": "dist/bin.js"
	},
	"typedoc": {
		"entryPoint": "src/index.ts"
	},
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/wighawag/etherfold.git",
		"directory": "platforms/nodejs-fetcher"
	},
	"dependencies": {
		"@etherfold/core": "workspace:*",
		"@etherfold/fetcher-host": "workspace:*",
		"ldenv": "^0.5.0",
		"named-logs": "^0.4.1",
		"named-logs-console": "^0.5.1"
	},
	"devDependencies": {
		"@etherfold/processor-sqlite": "workspace:*",
		"@etherfold/server": "workspace:*",
		"@hono/node-server": "^2.1.1",
		"@libsql/client": "^0.17.3",
		"@types/node": "^25.9.1",
		"as-soon": "^0.1.5",
		"remote-sql": "^0.0.7",
		"remote-sql-libsql": "^0.0.7",
		"typescript": "^6.0.3",
		"vitest": "^4.1.8"
	},
	"scripts": {
		"build": "rm -rf dist && tsc",
		"prepack": "cp ../../LICENSE LICENSE",
		"typecheck": "tsc -p tsconfig.typecheck.json",
		"dev": "as-soon -w src pnpm build",
		"test": "vitest run",
		"test:watch": "vitest"
	}
}
