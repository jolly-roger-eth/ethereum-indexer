# Derived from the committed stream fixtures by the spike's `run/verify-port.ts`,
# in seconds. The 12 MB trace regenerates identically and is not worth carrying;
# the golden STATE is committed (it is the oracle, and a diff on it means the
# processor changed meaning).
fixtures/*.trace.json
