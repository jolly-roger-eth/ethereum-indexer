{
	"name": "@etherfold/conformance-workload-stratagems",
	"version": "0.0.0",
	"private": true,
	"description": "The heavy conformance workload: a real launched processor, its real captured event stream, and the state the ORIGINAL processor computed from it, replayed through every state-store backend.",
	"type": "module",
	"license": "GPL-3.0-only",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/wighawag/etherfold.git",
		"directory": "packages/conformance-workload-stratagems"
	},
	"devDependencies": {
		"@etherfold/core": "workspace:*",
		"@etherfold/processor-entities": "workspace:*",
		"@etherfold/state-store": "workspace:*",
		"@etherfold/state-store-indexeddb": "workspace:*",
		"@etherfold/state-store-patch": "workspace:*",
		"@etherfold/state-store-sqlite": "workspace:*",
		"@libsql/client": "^0.17.3",
		"@types/node": "^25.9.1",
		"fake-indexeddb": "^6.2.2",
		"remote-sql-libsql": "^0.0.7",
		"tsx": "^4.21.0",
		"typescript": "^6.0.3",
		"viem": "^2.52.0",
		"vitest": "^4.1.8"
	},
	"scripts": {
		"build": "echo '@etherfold/conformance-workload-stratagems is test material: nothing to build'",
		"typecheck": "tsc -p tsconfig.typecheck.json",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:full": "STRATAGEMS_WORKLOAD=full vitest run",
		"test:all-backends": "STRATAGEMS_WORKLOAD=all vitest run"
	}
}
