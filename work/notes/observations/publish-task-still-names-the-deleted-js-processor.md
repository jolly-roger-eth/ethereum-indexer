# 2026-09-02: the publish task still names `@etherfold/js-processor`, and stratagems is written on it

`work/tasks/ready/publish-etherfold-and-deprecate-old-names.md` deprecates `ethereum-indexer-js-processor` towards `@etherfold/js-processor` (step 3) and gates on a clean-room install of "the browser + js-processor pair" (its acceptance list), but `retire-the-js-object-processor-path` deleted that package, so the forward pointer and the gate both name something that will never exist. Noticed while verifying package summaries against the real consumers: `wighawag/stratagems` still depends on `ethereum-indexer-js-processor` (its `indexer/src/index.ts` is a `fromJSProcessor` processor) and `ethereum-indexer-browser@^0.7.6`, so that task's "migrate stratagems to the new names" step is a PORT to the entity path rather than a rename, and the ported processor already exists as `@etherfold/conformance-workload-stratagems`.
