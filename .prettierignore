dist/
node_modules/
pnpm-lock.yaml
data/
docs/

# Retired packages kept only as reading material (see archive/README.md). They are
# outside the workspace globs, so nothing builds or tests them; reformatting them
# on a prettier upgrade would only add noise to code nobody is going to change.
archive/

*.md
*.yaml
*.html
*.json
